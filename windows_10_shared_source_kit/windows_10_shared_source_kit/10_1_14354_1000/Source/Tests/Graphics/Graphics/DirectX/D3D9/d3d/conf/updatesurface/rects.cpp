#include "rects.h"

SOURCE_DEST_RECORD SourceDestPairs[] =
{
	{{0, 0, 32, 32}, {0, 0}},
	{{0, 0, 32, 32}, {32, 32}}
};
CONST UINT cuiNoOfSourceDestPairs = sizeof(SourceDestPairs) / sizeof(SourceDestPairs[0]);
